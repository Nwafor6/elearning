from django.urls import path
from .import views






urlpatterns=[
	# path('student-sign-up/', views.StudentSignUpView.as_view(), name='student-sign-up'),
	# path('register-course/', views.LearnerCouseRegSerializerView.as_view(), name='register-course'),
	# path('edit-course/<str:pk>/', views.LearnerCouseeditSerializerView.as_view(), name='edit-course'),
	# path('answer', views.ModuleAnswerView.as_view(), name="answer")

]